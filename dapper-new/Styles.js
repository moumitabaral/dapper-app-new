import {StyleSheet} from 'react-native'

const PrimaryColor = "#AE8447"
const SecondaryColor = "#DDDDDD"

const styles = StyleSheet.create({
    promaryColor: PrimaryColor,
    secondaryColor: SecondaryColor,
    container: {
        flex: 1,
        backgroundColor: "#fff",
        paddingHorizontal: 14
    },
})

return styles